<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dilatua">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ReimpostaPasswordActivity"/>
        <activity
            android:name=".SfondoActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScegliTipologiaQuiz" />
        <activity android:name=".OpzioniDomandaActivity" />
        <activity android:name=".anteprimaActivity" />
        <activity android:name=".CreaDomandaActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".ProfiloActivity" />
        <activity android:name=".RegistrazioneActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>